{% extends 'base.html.twig' %}

{% block titre %}
    {{ parent() }} | Profil
{% endblock %}

{% block body %}
    <div id="">
        <h2>{{ participants.nom }}</h2>
    </div>
    <img src="">
    <div class="affiche_profil">
        <div> Prénom : {{ participants.prenom }}</div>
        <div> Nom : {{ participants.nom}}</div>
        <div> Télephone :  {{ participants.telephone  }}</div>
        <div> Email : {{ participants.mail  }}</div>
        <div> Campus : {{ participants.campus.nom }} </div>

    </div>
    <a href="{{ path('sortie_liste') }}"><button id="btnCreer">Retour</button></a>




{% endblock %}


